import React, { Fragment } from "react";
import ListeFilm from "../ListeFilm";
export const PageFormulaire = (props) => {
  return (
    <Fragment>
      <ListeFilm />
    </Fragment>
  );
};

export default PageFormulaire;
